[project]
name = "dsgl"
version = "1.0.0"
description = "DSGL - Dreamfinity Simple GUI Library"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "mkdocs",
    "mkdocs-glightbox",
    "mkdocs-material",
    "mkdocs-material-extensions",
]
